#define $skip_cases() scanf("%*d")
#define $to_eof(...)  while(~(__VA_ARGS__))
#define $for(i, a, b) for(auto i = a; i < b; ++i)
typedef int $default_type;
typedef $default_type $node_type;
#define P [&]($node_type node)
#define L vector<$node_type>
#define pi acos(-1.0)
